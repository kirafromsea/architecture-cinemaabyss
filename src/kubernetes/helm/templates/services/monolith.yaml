{{- if .Values.monolith.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-monolith
  namespace: {{ .Values.global.namespace }}
  labels:
    app: monolith
    chart: {{ include "cinemaabyss.chart" . }}
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.monolith.replicaCount }}
  selector:
    matchLabels:
      app: monolith
  template:
    metadata:
      labels:
        app: monolith
    spec:
      {{- if .Values.imagePullSecrets.dockerconfigjson }}
      imagePullSecrets:
      - name: {{ .Release.Name }}-dockerconfig
      {{- end }}
      containers:
      - name: monolith
        image: "{{ .Values.monolith.image.repository }}:{{ .Values.monolith.image.tag }}"
        imagePullPolicy: {{ .Values.monolith.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.monolith.service.port }}
        env:
        - name: PORT
          value: {{ .Values.monolith.env.PORT | quote }}
        - name: DB_CONNECTION_STRING
          value: "postgres://postgres:$(DB_PASSWORD)@{{ .Release.Name }}-postgresql:5432/cinemaabyss?sslmode=disable"
        envFrom:
        - configMapRef:
            name: {{ .Release.Name }}-config
        - secretRef:
            name: {{ .Release.Name }}-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.monolith.service.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.monolith.service.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-monolith
  namespace: {{ .Values.global.namespace }}
  labels:
    app: monolith
    chart: {{ include "cinemaabyss.chart" . }}
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.monolith.service.type }}
  ports:
  - port: {{ .Values.monolith.service.port }}
    targetPort: {{ .Values.monolith.service.port }}
    protocol: TCP
  selector:
    app: monolith
{{- end }}